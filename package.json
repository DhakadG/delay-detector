{
  "name": "delay-detector",
  "version": "0.1.0",
  "description": "Measure real audio output latency in the browser and get a player sync offset",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node test/engine.test.js && node test/batch.test.js && node test/bridge.test.js && node test/vmsync.test.js",
    "lint": "eslint src test",
    "stamp": "node scripts/stamp.mjs",
    "deploy": "npm run test && npm run stamp && wrangler deploy",
    "start": "npx serve ."
  }
}
